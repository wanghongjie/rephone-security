/// Central place to configure backend endpoints.
/// Adjust these values per environment (dev / staging / prod).
const String defaultAuthHost = 'rephone.top';
const int defaultAuthPort = 8086;
const bool defaultAuthUseHttps = true;
